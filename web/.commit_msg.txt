feat: analytics overhaul, streaming sync, magalu fulfillment and fixes

Analytics:
- DateRangePicker with presets (Hoje, 7d, 30d, Este mes, Custom)
- AnalyticsFilters: client + channel + logistics filters
- Portfolio Overview: BarChart 12 meses, ChannelRankingCard em ranking
- Client Dashboard: BarChart horizontal + Donut (canais e logistica)
- Share page: filters added
- Brand colors: blue to violet fix (6 locations)

Sync:
- erp-stream-sync: new SSE-based streaming sync with full pagination
- SyncProgressPanel: real-time progress with order badges and counters
- SyncStatusBadge: violet badge in header during active syncs
- SyncDialog: streaming instead of queue enqueue
- sync_queue: date_from, date_to, synced_count columns
- erp-process-sync-queue: uses date range from queue items
- erp-enqueue-sync: accepts date_from and date_to
- SyncQueueNotifier: shows synced count in toast

Magalu Fulfillment:
- parseOrder detects fulfillment via notaFiscal.id=0 and servico Magalu
- Migration: 119 orders backfilled
- Status label rule: 'Em aberto' to pending

Chat:
- Model selector moved to input area
- History sidebar with search, pin, delete
- Split-screen expanded view
- Report HTML hidden during streaming
- Parallel tool calls and tool_status SSE events
- Database indexes for chat performance

Fixes:
- Date priority: data (order) over dataEmissao (invoice)
- erp_marketplaces: GRANT SELECT to authenticated
- Sync badge: only shows items under 30min old
- CORS headers on SSE response
- Cross-schema join fix in stream-sync
